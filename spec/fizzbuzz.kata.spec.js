const sut = require("../src/fizzbuzz.kata");
xdescribe(`fizzbuzz kata`, () => {
    it(`should export the setup function`, () => {
      // Arrange
      // Act
      expect(sut.setup).toBeAFunction();
      // Assert
    });
    xit(`should create the sandbox area`, () => {
      // Arrange
      // Act
      // Assert
    });
});